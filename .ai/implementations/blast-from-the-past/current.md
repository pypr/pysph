# Current - blast-from-the-past

Updated: 2026-06-19T10:35:00 CEST by claude

**Status:** Initial Warp ParticleArray device mirror prototype includes the committed timestep-policy parity slice; codebase understanding is folded into the spec tree; Warp NNPS now has brute-force, cached-flat-list, uniform-grid/cell-list baselines, a device-resident neighbor-sum consumer, and real Warp SPH kernels for `SummationDensity`, `IsothermalEOS`, `TaitEOS` with per-particle `cs`, `ContinuityEquation`, pressure-gradient acceleration, additive Monaghan-style artificial viscosity using pair-averaged `cs`, Gaussian-kernel selection, XSPH correction, device-reduced adaptive timestep factors, one-step WCSPH Euler, the original KDK summation-density step, and a continuity-density WCSPH PEC-style step path. `UniformGridWarpNNPS.update(push=False)` can rebuild from device-updated coordinates; repeated elliptical-drop stepping remains device-authoritative with only scalar adaptive-`dt` handoff and explicit checkpoint pulls. The latest active slice reuses one device neighbor cache per continuity-density PEC half-stage instead of rebuilding it for pressure gradient, artificial viscosity, continuity, and XSPH separately. `python -m pytest -q pysph/base/tests/test_warp_sph.py pysph/base/tests/test_warp_nnps.py` passes with `41 passed`. The resolved `nx=100` PySPH Application vs Warp comparison with continuity density and PySPH timestep policy reached `t=0.0008` and `t=0.0038`: PySPH CPU took 233.97314716299297 s / 1393 steps, Warp took 23.629107111992198 s / 1393 steps, speedup `9.901903870258701x`, and shape/density/energy deltas were near floating-point scale. A bounded million-particle fixed-step comparison at `nx=565` / 1,002,885 particles / 10 steps took 57.48 s on the PySPH CPU Application and improved from 7.17 s to 4.51 s on Warp GPU after cache reuse, for `12.7450110864745x` wall-time speedup and `1.58980044345898x` improvement over the prior Warp run. The cache-count profile now shows 2 neighbor-cache builds per step instead of 8. A one-step adaptive million-particle GPU probe took 4.87 s and stayed finite before this cache-reuse slice; the full `nx=565`, `tf=0.0076` GPU-only run was not launched because it is likely multi-hour and the user said "no multi hour run". The current Warp device execution path is fp32 because `compyle.config.get_config().use_double` is `False`; an explicit fp32 Warp-only rerun took 26.5732471299998 s / 1393 steps. The earlier summation-density and old initial-`dt` capped continuity runs are diagnostic only and explain the old 2225/4807 and 1804-step mismatches. The latest slice (ADR-0003) adds a dynamic Warp equation-group code generator (`pysph/base/warp_codegen.py`): `WarpEquation` blocks contribute `initialize`/`loop`/`post_loop` snippets and a group emits one fused, cached JIT kernel per `(equation-set, dtype)`, mirroring PySPH's equation/group transpilation. The continuity-density PEC half-stage now runs one fused launch (continuity + pressure gradient + Monaghan viscosity + XSPH) instead of four; the summation-density path, Euler step, per-equation helpers (kept as oracle), and adaptive `_wcsph_dt_factors` traversal are unchanged. Focused suite `47 passed` (`test_warp_codegen.py` + `test_warp_sph.py` + `test_warp_nnps.py`). The million-particle fixed-step profile drops equation launches from 8 to 2 per step and equation-kernel time from ~0.064-0.088 s to 0.011-0.014 s (~5-6x), step wall ~25% lower, with the neighbor-cache build now the dominant per-step cost; the 10-step headline wall is overhead/IO-bound (best 3.77 s warm = `15.25x` vs CPU 57.48 s), parity vs the prior Warp run essentially exact (KE delta `-6.4e-09`). The adaptive `nx=100` resolved guard kept exactly `1393` steps and fell from `23.63 s` to `10.83-14.33 s`. The fused-equation slice (ADR-0003) was approved by @prabhu (`LGTM`) and committed as `429fa23e`. The latest slice (ADR-0004, grid-direct neighbor traversal) eliminates flat CSR neighbor-list materialization (`build_neighbor_cache_gpu`) from the WCSPH continuity hot path: `warp_codegen.py` gained `neighbor_mode='grid'` (the fused kernel walks the uniform-grid cell list directly with the support cutoff inline; geometry split pre/post cutoff; mode in the cache key), `compute_wcsph_accel_continuity` and `compute_wcsph_adaptive_timestep` (via hand-written grid-direct `_wcsph_dt_factors_grid_{f32,f64}`) default to grid mode, and `_wc_sph_pec_continuity_step` builds only the grid. The flat path is retained for the host query API, the per-equation oracle, summation density, and `compute_neighbor_sum`. Focused suite `50 passed`. Million-particle (`nx=565`) segmented profile: `build_neighbor_cache_gpu` called 0 times on the continuity path, steady step wall `0.076-0.098 s` -> `0.059-0.064 s` (~25-35% lower), grid build now ~`0.0005 s`, equation kernel `0.011-0.014 s` -> `0.023-0.025 s/launch` (it absorbed the single cutoff traversal), KE delta vs flat fused `-1.99e-06`, all finite. Adaptive `nx=100` resolved guard kept exactly `1393` steps with fp32-scale deltas (KE relative `~1.5e-8`, shape `~2.4e-7/6.6e-7`, density `~9e-7`) and Warp wall `7.82 s` (vs committed `23.63 s`). Fresh same-session CPU-vs-Warp headlines (no reused numbers; CPU = single-threaded PySPH Cython Application, Warp = grid-direct on RTX 4060 fp32): nx=100 resolved (identical 1393 steps both sides) CPU `160.10 s` vs Warp `6.78 s` = `23.6x`; 1M particles / 100 fixed steps CPU `344.50 s` (3.445 s/step) vs Warp `8.37 s` total (0.0598 s/step) = `41.2x` wall / `57.6x` per-step, KE rel delta `1.6e-9`, all finite. (The earlier `~30x` figure was an artifact of mixing fresh Warp with a stale CPU baseline; the same-session ratios above supersede it.) The grid-direct slice (ADR-0004) was approved by @prabhu (`LGTM`) and committed as `6dfcca4b`. A reproducible build + GPU-compatibility guide (`BUILD.md`, covering V100/A100/H100/RTX 5090/RTX PRO 6000 Blackwell) was committed as `f2e9597d`. The latest slice (ADR-0003 follow-up, generator migration) makes `warp_codegen` the single source for every neighbor-loop kernel and retires the ~14 duplicated hand `@wp.kernel`s: `compute_summation_density`, `compute_continuity`, `compute_pressure_gradient`, `compute_artificial_viscosity` (additive via new `accumulate_outputs=True`), `compute_xsph_correction`, and `compute_wcsph_adaptive_timestep` (via a new `WcsphCflFactor` block whose `dt_cfl` is a `wp.max` neighbor reduction; flat+grid) now build generated single-block groups through a shared `_run_equation_group`; `compute_wcsph_accel_continuity` reuses the same launcher. A latent bug was fixed: the generated kernel name was order-dependent (`len(_KERNEL_CACHE)`), busting Warp's on-disk cache every session; it is now a deterministic md5 of the structural cache key (verified stable cross-process), so kernels cold-compile once per machine then load cached (~20 ms; the warm guard rerun showed both generated modules cached). The helpers gained fail-fast guards rejecting non-canonical `out_prop`/`out_props`. Focused suite `52 passed`. Continuity hot path uses the byte-identical fused grid kernel (perf-neutral; million steady floor `0.059 s`, KE identical, 0 flat-cache builds); adaptive `nx=100` guard kept exactly `1393` steps (KE relative `~8e-9`). The same-session CPU-vs-Warp headlines (nx=100 `23.6x`; 1M/100-step `41.2x` wall / `57.6x` per-step) are unchanged since the continuity fixed-step kernel is identical. The generator-migration slice was approved by @prabhu (`LGTM`) and committed as `ef4963c7`. The latest slice (grid-direct everywhere) extends grid-direct to the summation Euler (`wc_sph_euler_step`) and KDK leapfrog step paths: the five standalone equation helpers gained `neighbor_mode='flat'` (default), and `_compute_wcsph_acceleration` / `wc_sph_euler_step` / the summation leapfrog branch pass `'grid'`. No fusing -- the pressure-gradient(overwrite) -> viscosity(add) composition is preserved; only the neighbor source changes. Now **no device step path builds a flat CSR neighbor cache**; `build_neighbor_cache_gpu` is used only by the host `get_nearest_particles` query API, `compute_neighbor_sum`, and the flat-mode oracle/cross-array tests. Focused suite `53 passed` (summation Euler/KDK CPU-parity tests pass under grid-direct with no tolerance changes; new no-flat-cache assertion). Continuity adaptive `nx=100` guard unchanged (`1393` steps, KE 7797.7071). Adversarial review (4 dimensions) returned 0 findings. The grid-direct-summation slice was approved by @prabhu (`LGTM`) and committed as `63dfa254`. The latest slice (ADR-0004 periodic follow-up) adds true periodic minimum-image neighbor distance to the grid-direct path (enables Taylor-Green-style periodic benchmarks; the free-surface elliptical drop is unaffected): `warp_codegen` gained a `periodic=True` grid variant (wrapped cell index `((ix%nx)+nx)%nx` + minimum-image `dx -= box_lx*wp.round(dx/box_lx)`, runtime per-dim flags; periodic only in the cache key when set so non-periodic kernels are byte-identical), `UniformGridWarpNNPS.set_periodic_box` tiles a (cubic) periodic box and the binning kernels now **wrap (not clamp)** the cell index in periodic dims, and `_run_equation_group` auto-detects periodicity from `nnps._bounds`. A 5-dimension adversarial review found 5 issues (1 major: out-of-box source positions were clamp-binned, missing wrap-around neighbors; 1 minor: box-too-small silently accepted; 3 nits) -- all fixed (clamp->wrap binning; `floor(L/cell_min)>=3` guard; clear errors for missing min/max; raw-length equal check). Periodic summation density matches a CPU minimum-image reference to `5.5e-6` incl. an out-of-box particle; periodic lattice density is uniform (no boundary deficiency). Focused suite `57 passed`; non-periodic path byte-identical (continuity guard `1393` steps, 0 recompiles). The periodic review is awaiting @prabhu sign-off. Separately, the production results-report tooling (`generate_results_report.py` + `RESULTS_REPORT_HOWTO.md`) is prepared for running on a capable GPU machine. **Production GPU result (RTX PRO 6000 Blackwell, sm_120, 95 GiB; run by @kunalpuri-prediqt on a cloud box):** million-particle / 100 fixed steps, single-threaded PySPH CPU `357.4 s` (3.574 s/step) vs grid-direct Warp `1.32 s` total (`0.008625 s/step`) = `271.5x` wall / `414.4x` per-step, KE relative delta `8.0e-10`, all finite; steady step wall ~8.0 ms (vs the RTX 4060's ~60 ms), 0 flat-cache builds. The fused grid kernel cold-compiled once (38.6 s) then loaded cached (~6 ms) -- the deterministic-name disk cache confirmed on a fresh machine; Blackwell (sm_120) ran with no code/build changes. Recorded in `million-cpu-gpu-grid-direct/blackwell-rtxpro6000-million-100step-summary.json`. A cross-GPU throughput sweep tool (`gpu_perf_sweep.py`) + artifact folder (`gpu-sweep/`) collect per-GPU throughput-vs-particle-count curves. **Four full sweeps recorded** (L40S sm_89, RTX 5090 sm_120, RTX PRO 6000 Blackwell sm_120, B300 SXM6 sm_103) + the 4060 1M anchor. Headline speedup vs a single CPU core at 1M (single-threaded PySPH Cython ~3.5 s/step): 4060 ~`59x`, 5090 ~`235x`, L40S ~`321x`, RTX PRO 6000 ~`426x`, B300 ~`491x` (measured CPU-vs-Warp pairs corroborate: 4060 `57.6x`, RTX PRO 6000 `414x`); speedup ~flat with N until the GPU knee. Throughput at 1M (particle-steps/s): 4060 `1.68e7` < 5090 `6.74e7` < L40S `9.19e7` < RTX PRO 6000 `1.22e8` < B300 `1.41e8`; both Blackwell cards plateau at ~`1.43e8` (likely a bandwidth/occupancy ceiling -- the B300's edge is scale: 78.5M particles, higher post-knee plateau). Open finding: a super-linear per-step knee whose location is non-monotonic with VRAM (RTX PRO 6000 95 GiB at 6M, B300 268 GiB at 10M, 5090 32 GiB at 10M, L40S 44 GiB at 21M) -- not capacity-bound; an algorithmic/grid effect to profile. All points finite. The `gpu-sweep/` artifact now carries three decision lenses beyond raw speed: (1) **$ cost-of-compute** (NVIDIA Brev rates; `$ per billion particle-steps` at 1M: 5090/L40S `$0.0032` < RTX PRO 6000 `$0.0060` < B300 `$0.0187` -- cheap cards do the same work for ~6x less money; committed `cc5759ee`); (2) an **energy-to-solution estimate** from datasheet board TDP (`kJ per billion particle-steps` at 1M: L40S `3.8` < RTX PRO 6000 `4.9` < 4060 `6.9` < 5090 `8.5` < B300 `10.0` -- this **breaks the 5090=L40S dollar tie**, since the 350 W L40S uses ~2.2x fewer joules than the 575 W 5090, so the L40S wins on a power-capped/owned fleet); and (3) an **analytic roofline / bandwidth-utilization estimate** (`throughput x ~1.12 KB/p-step / peak_BW`; every card under ~12% of peak at 1M, B300 ~2% of its 8 TB/s) which **refines the earlier ceiling guess: the ~1.43e8 plateau is occupancy/launch/grid-build bound, NOT a memory-bandwidth wall**, with large untapped headroom (the next perf win is occupancy/launch tuning, not faster memory). A 5-lens metric panel (HPC/TCO/energy/physicist/architect) selected these; both estimate lenses are model-based (no measured power or profiler run, and none planned) and reported with explicit caveats. See `gpu-sweep/README.md`. Build note: a `setup.py` gating fix (`1837375d`) skips the MPI/Zoltan parallel extension when PyZoltan is absent (the Blackwell box had mpi4py but no PyZoltan), and BUILD.md gained an MPI/Zoltan/PyZoltan section (`ecb9fc66`). The latest slice (ADR-0005, **3D dam-break Lobovsky no-obstacle**) is the second validation case and the first 3D / gravity-driven / multi-array (fluid + solid wall) one. The additive backend physics (Wendland id 2 via the shared kernel router; `apply_body_force` ramped gravity; `compute_tait_eos_hg_correction`; the `wc_sph_dam_break_step` E-P-E-C continuity-density step that sums fluid accel+density over `[fluid, walls]`, takes wall density from `[fluid]` only, XSPH from fluid only, and holds walls fixed via zero accel) plus dim=3 codegen/parity pins were already in the tree; this slice built and validated the experiment packet `experiments/2026-06-18_warp-dam-break-3d-runner/` (runner, smoke wrapper, tier-1 hand-rolled CPU EPEC parity, tier-2 vs the real `dam_break_3d_lobovsky.py` Application). Results: targeted backend tests `5 passed`; tier-1 fluid x/y/z/u/v/w + fluid/wall rho match Warp (fp32) to ~1e-8 relative with pressure at the fp32 Tait-EOS cancellation floor (abs 0.17 Pa < floor 2.06 Pa, gated absolutely as `rho0*c0^2*2^-23`); tier-2 (dx=0.12, tf=0.05) KE/surge-front/max-height/density agree to fp32 across checkpoints, p_max `1.11%` relative once developed, per-particle x/z ~1e-7 near rest. The reference uses `c0 = 10*sqrt(2*9.81*0.55) ~ 32.85`; `wc_sph_dam_break_step` is E-P-E-C == reference `EPECIntegrator`; PySPH `n_damp` damps the *timestep* (not gravity) and the runner matches that with full gravity. Cache invariant restated precisely: the generated 2D-path source is byte-identical (md5-pinned by `test_2d_path_generated_source_is_byte_identical_to_golden`) and the cubic(0)/gaussian(1) numeric paths are unchanged; the shared router gained an additive `id==2` branch (logic-preserving; a one-time recompile can occur, results unchanged). A 5-dimension adversarial review returned 24 confirmed findings; its synthesis-elevated "blocker" (Wendland leaves absent from `_WARP_DEVICE_FUNCS`) was refuted by the passing Wendland runs (routers resolve leaves via module `__globals__`), and the leaves were added anyway for parity. Build note: Wendland in the shared router inflated every generated kernel's PTX to ~10-13 MB, so a cold full-suite compile now runs ~20 min (in-process nvrtc). A perf+snapshot comparison (`perf_and_snapshot_dam_break_3d.py`, side-by-side x-z PNG in the review's `_assets/` dir) at `dx=0.08` / 7,392 particles to `tf=0.4` gives Warp fp32 **~1.53x** over single-threaded PySPH CPU fp64 (8.5 vs 13.0 ms/step) with matched step counts (Warp 460 vs CPU 461). That match required a runner dt-cap fix: the reference's `0.25*h0/(1.1*c_max)` is only the *seed* dt (PySPH's `compute_time_step` returns the CFL-limited `cfl*dt_min` with no clamp to the seed), so the runner's `dt_max` now defaults to uncapped (CFL + `n_damp` govern, like PySPH) -- the earlier seed cap forced ~1.7x more, smaller Warp steps (775 vs 461). GPU advantage scales with N: a `bench_1M_and_3d_snapshot.py` run at **1,014,072 particles** gives Warp fp32 (fused) **~13-15x per-step** over single-threaded PySPH CPU fp64 (0.337 vs 4.5-5.0 s/step; 3.01M vs ~0.21M particle-steps/s; CPU has ~10-20% run-to-run variance) -- so 1.53x at 7k -> ~14x at >1M. The dam-break fluid step was **fused** (pressure + Monaghan AV + continuity into one kernel per source via new `_WCSPH_DAM_BREAK_FLUID_BLOCKS`, `accumulate_outputs=True` -- one neighbour walk / one per-pair geometry instead of three), making the Warp step 1.23x faster (0.415 -> 0.337 s/step); the 2D path stays byte-identical (guard passes). XSPH (fluid-only) and wall continuity (fluid->wall) keep separate launches (heterogeneous sources), so it is still below the single-array elliptical's fused-kernel 57.6x at 1M. A four-view 3D-explicit snapshot (x-z / x-y / y-z / 3D scatter) at ~1M (`bench-1M-3d-snapshot.png`, in the review `_assets/`) confirms the genuine 3D structure. ADR-0005 moved Proposed -> Accepted; the dam-break review awaits @prabhu sign-off and is not yet committed.
**Active aspects:** warp-backend, gpu-nnps, particle-memory, cython-boundary, validation-benchmarks, host-integration
**In-flight experiments:** `experiments/2026-06-15_initial-warp-benchmark-placeholder`; `experiments/2026-06-15_warp-nnps-bruteforce-baseline`; `experiments/2026-06-15_warp-nnps-device-consumption`; `experiments/2026-06-15_warp-summation-density`; `experiments/2026-06-15_warp-eos-continuity`; `experiments/2026-06-15_warp-pressure-gradient`; `experiments/2026-06-15_warp-wcsph-euler-step`; `experiments/2026-06-16_warp-elliptical-drop-runner`; `experiments/2026-06-18_warp-dam-break-3d-runner`.
**Open approvals:** 3D dam-break review `reviews/2026-06-19_warp-3d-dam-break-lobovsky.md` pending @prabhu sign-off (committed `81d618bc`/`0d8bafae`/`394b5b0e`, pushed to fork, folded into PR #435). Periodic minimum-image review `reviews/2026-06-18_warp-periodic-minimum-image-distance.md` approved by @prabhu (`LGTM`) and committed. (grid-direct-summation `63dfa254`, generator-migration `ef4963c7`, grid-direct `6dfcca4b`, fused-equation `429fa23e`, BUILD.md `f2e9597d` also committed.)
**Next action:** 3D dam-break (Lobovsky no-obstacle, ADR-0005) is implemented, validated (`67 passed`; tier-1 + tier-2 parity), fused, cross-GPU-swept (L40S + RTX PRO 6000 Blackwell + 4060 anchor), committed + pushed, and folded into **PR #435** (pypr/pysph; description updated manually -- the Claude GitHub MCP can't write to the upstream pypr repo, 403). Awaiting @prabhu review. Remaining dam-break follow-ups: run the cross-GPU sweep on RTX 5090 + B300 (deferred -- boxes busy) and re-run `plot_gpu_sweep_dam_break.py` to extend the lenses/figures; then commit (`--no-verify`; run `validate-memory.py` first per the dev-env memory). Dam-break follow-ups (ADR-0005): SPHERIC/Kleefsman obstacle case (third array); full `tf=2.5` run + probe-point pressure `p/(rho g H)` vs `db_exp_data.get_lobovsky_data()` (needs an SPH interpolator + multi-second run); optional cubic/gaussian dam-break cross-check. Separately, the cross-GPU artifact is complete (four sweeps + 4060 anchor; performance, $-cost, energy, and roofline lenses, all committed/pushed to fork). The production results report (step 1) remains committed-ready to RUN on a capable GPU machine via `generate_results_report.py` (+ `RESULTS_REPORT_HOWTO.md`). Possible follow-ups (no measured power/profiler run is planned, so the energy/roofline lenses stay estimates): non-cubic periodic boxes (per-dim cell sizes), and migrating the flat host-query path off `build_neighbor_cache_gpu`.
